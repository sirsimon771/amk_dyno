this folder should contain some files named Algorithm.cs with some extensions
that explain what this specific Algorithm does.

It is intended as an archive for old code as well as a place to put stuff to test
next time the program is used.